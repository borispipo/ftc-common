export const open = x=>null;
export const close = x=>null;
export const showPreloader = open;
export const hidePreloader = close;

export default {
    open,
    close,
    showPreloader,
    hidePreloader
}